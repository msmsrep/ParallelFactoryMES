# 製造実績管理システム 仕様書（ASP.NET Core バックエンド + Blazor Webクライアント + WPFライセンスアプリ版）
プロダクト名：Parallel Factory MES

作成日: 2026-07-15（改訂4：MES機能をWebクライアントに一本化。デスクトップMESクライアントを廃止し、Windows Store配布アプリはWPF製のライセンス購入・登録専用アプリに変更。シートはPC（端末）単位の割当に変更）
改訂5: 2026-07-16（ロット・BOMをデータモデルに追加しトレーサビリティ／部材自動消費の根拠を定義。シート購入を複数SKU方式に変更。購入登録の一意キーをアプリ生成の購入トラッキングIDに変更し、購入リカバリ手順を拡充）
改訂6: 2026-07-16（シート調整（返金対応）エンティティ追加、アクティベーションコード失効時のシート解放を定義、「端末」＝ブラウザプロファイルの定義明確化、JWT保管・リフレッシュ・CSRF対策の規定、初期管理者セットアップの定義、Storeポリシー適合の事前確認をフェーズに追加）
改訂7: 2026-07-16（.NET 10（LTS）へ変更、製造日（業務日付）境界の定義、工順の方針（工程ごとに指示分割）、在庫更新の楽観的同時実行制御、アクティベーションコードの形式・レート制限、バックアップ・リストア方針（7.7節）を追加）
参考: みんなのMES（min-MES） https://min-mes.com/ / OSS: https://github.com/mihatama/open-mes-project

---

## 1. プロジェクト概要

### 1.1 目的
製造現場における生産実績・在庫・品質・設備情報を一元管理するオールインワン型MES（製造実行システム）を開発する。オープンソースの「みんなのMES」を機能面の参考とする。バックエンドはASP.NET Core APIに一本化し、データベースはバックエンドのみが保持する単一DBとする。

MES機能のクライアントは**Webブラウザ（Blazorクライアント）のみ**とする。これとは別に、Windows Store配布の**WPF製ライセンスアプリ**を用意するが、これはシート（利用ライセンス）のStore購入とバックエンドへの登録処理のみを担い、MES業務機能は一切持たない。

### 1.2 対象ユーザー
- 現場作業者（実績入力、作業指示確認）
- 生産管理担当者（作業指示発行、進捗管理）
- 品質管理担当者（検査記録、不良分析）
- 設備保全担当者（設備稼働履歴、保全記録）
- システム管理者（マスタ管理、ユーザー管理、シート・端末管理）

### 1.3 機能スコープ
みんなのMES同等の4本柱＋共通基盤：
1. 生産管理（作業指示・実績・進捗）
2. 在庫管理（入庫・在庫・出庫）
3. 品質管理（検査記録・分析）
4. 設備管理（稼働履歴・資産管理）
5. 共通基盤（認証・ユーザー管理・ダッシュボード・バーコード/QRスキャン・シート/端末管理）

### 1.4 製品構成・課金モデル

#### 課金モデル：PC（端末）単位のシート制

課金の単位は**端末（PC）**であり、MESユーザーアカウントとは独立とする。

| 原則 | 内容 |
|---|---|
| シートの単位 | 1シート＝端末1台。シートが割り当てられた端末からのみWebクライアント（MES機能）を利用できる。 |
| 「端末」の定義 | 端末の実体は**デバイストークン（Cookie）を保持する特定PC上の特定ブラウザプロファイル**である。同一PCでも別ブラウザ・別プロファイルは別端末扱いとなるため、利用ブラウザは端末ごとに1つに固定して運用する（運用手順書に明記）。 |
| MESアカウントとの関係 | シートはMESユーザーアカウントとは紐づかない。**シート割当済みの端末では、複数のMESユーザーアカウントが利用可能**（アカウント数無制限）。 |
| 端末の排他 | **同一シートを複数の端末で同時に使うことは不可**。1シートは常に最大1台の端末に割当。端末の故障・入替時は管理者が割当を解除し、別端末に再割当する（解除時点で旧端末のアクセスは無効化）。 |

#### 購入〜利用の流れ

1. Windows Storeに「シート ×1」「シート ×5」「シート ×10」など**複数SKUの消費型（Consumable）In-App Purchaseアドオン**として登録する（Store IAPは1回の購入で数量を指定できず1トランザクション＝1アドオンのため、まとめ買いはSKUの粒度で対応する）。
2. 管理者が任意のWindows PC上の**WPFライセンスアプリ**（Windows Store配布）からSKUを選択してシートを必要数購入する（`Windows.Services.Store` API。必要数はSKUの組合せ・複数回購入でまかなう）。
3. WPFライセンスアプリは購入結果（**アプリ生成の購入トラッキングID**・SKU・シート数）をMesApp.Apiに送信し、バックエンドに**未割当シート**としてプールする。WPFアプリの役割はここまで（購入と登録のみ）。
4. 管理者がWebの管理画面から**端末アクティベーションコード**を発行し、利用したいPCのブラウザでコードを入力して端末をアクティベートする（シート1消費）。以降、その端末のブラウザにはデバイストークンが保持され、端末が識別される。
5. アクティベート済み端末からは、任意のMESユーザーアカウントでログインしてMES機能を利用できる。**Webクライアント・MES利用時にWindows Storeとの通信は一切発生しない**（判定材料はすべてバックエンドDB内）。

この方式により、決済は100% Windows Store経由で完結しつつ、MESの日常利用にはWindowsストア・Microsoftアカウント・デスクトップアプリのいずれも不要となる。

#### レシート検証・返金の方針

- バックエンドはローカル（インターネット非接続もあり得る）環境での稼働を想定するため、Storeレシートのサーバーサイド検証（Microsoft Store collections API）は行えない。初期リリースではWPFライセンスアプリからの購入報告を信頼する（オンプレB2B環境では偽装に顧客側の意図的な不正を要するためリスク許容とする）。将来オプションとして、WPFアプリ（ネット接続可）→ ベンダー運用のライセンスサービス（Storeレシートを検証し署名付きエンタイトルメントを発行）→ バックエンドが埋め込み公開鍵で署名検証、という経路の追加を検討する（7.1節参照）。
- Consumableアドオンは購入即消費のため、Store側の返金を自動でバックエンドに反映する経路はない。返金対応が生じた場合は管理画面から**シート調整（SeatAdjustment、マイナス調整）**を記録し、必要に応じて割当解除を併用して運用する（5章参照）。

---

## 2. システムアーキテクチャ

バックエンド（ASP.NET Core API + DB）を中心に、MES機能クライアントはBlazor Webクライアントのみ。WPFライセンスアプリはシート購入・登録専用の補助アプリとして独立させる。データアクセスはすべてAPI経由のみで、DBには直接触れない。

```
┌──────────────────────────────┐   ┌──────────────────────────────┐
│ MesApp.Client.Web              │   │ MesApp.LicenseApp              │
│ (Blazor WebAssembly)           │   │ (WPF / Windows Store配布)       │
│  MES機能の唯一のクライアント      │   │  シート購入(Store IAP)と        │
│  シート割当済み端末のみ利用可     │   │  バックエンドへの購入登録のみ     │
│  ブラウザからアクセス            │   │  MES業務機能なし                │
└──────────────┬─────────────────┘   └──────────────┬───────────────┘
               │ HTTPS / REST API                    │ HTTPS / REST API
               └──────────────┬──────────────────────┘
                              ▼
                 ┌─────────────────────────┐
                 │  MesApp.Api               │
                 │ (ASP.NET Core Web API)    │
                 │ 認証(JWT等)・業務ロジック   │
                 │ シート/端末管理・ライセンス検証│
                 │ Webクライアントの静的配信も担う│
                 └────────────┬─────────────┘
                              ▼
                 ┌─────────────────────────┐
                 │   単一DB（バックエンドのみ保持）│
                 │  既定: SQLite／設定で変更可     │
                 │  (PostgreSQL, SQL Server 等)  │
                 └─────────────────────────┘
```

### 2.1 各コンポーネントの役割
- **MesApp.Api（バックエンド）**: 唯一のデータアクセス主体。認証、業務ロジック（在庫整合性チェック、実績集計など）、シート・端末管理、ライセンス検証をすべて担う。MesApp.Client.Webの静的ファイル配信も行う。DBは1つのみ保持し、クライアントからの直接アクセスは行わせない。
- **MesApp.Client.Web**: Blazor WebAssemblyとして実装し、ブラウザからAPIを呼び出すMES機能の唯一のクライアント。シート割当済み（アクティベート済み）端末からのみ利用可能。
- **MesApp.LicenseApp**: Windows Store配布のWPFアプリ。役割は「Consumableアドオン（シート）の購入」と「購入結果のMesApp.Apiへの登録」のみ。バックエンドURL設定と管理者認証の画面を持つ。MES業務機能・シート割当機能は持たない。
- **MesApp.Core**: ドメインモデル・DTO・APIコントラクト（リクエスト/レスポンス型）。API・Webクライアント・ライセンスアプリで共有。

### 2.2 認証・ライセンス・端末アクティベーションの流れ

#### A. シート購入（WPFライセンスアプリ）
1. 管理者がWPFライセンスアプリを起動し、バックエンドURLを設定のうえ管理者アカウントでログインする（JWT取得）。
2. 購入するSKU（シート ×1／×5／×10）を選択し、`StoreContext.RequestPurchaseAsync`でConsumableアドオンを購入する。1回の購入は1トランザクション＝1アドオンであり数量指定はできないため、必要数はSKUの組合せ・複数回購入でまかなう。
3. アプリは**購入開始前に購入トラッキングID（アプリ生成GUID）とSKUをローカルに永続化**し、購入成功後にトラッキングID・SKU・シート数をMesApp.Apiに送信して未割当シートプールに加算する。`StorePurchaseResult`はStore発行のトランザクションIDをアプリに公開しないため、二重登録防止の一意キーは**このアプリ生成トラッキングID**とする（API側で一意制約とし、同一IDの再送は冪等に成功応答を返す）。
4. API登録成功後、`ReportConsumableFulfillmentAsync`（trackingIdに同じGUIDを指定）でStoreへ消費完了を報告する。
5. リカバリ処理（アプリ起動時に毎回実行）：
   - ローカル記録上「API登録済み・消費報告未完了」の購入は、消費完了報告を再送する。
   - `GetConsumableBalanceRemainingAsync`でStore側の未消費残高を照会し、ローカル記録と突合する。API未登録の残高（購入成功後・API登録前にアプリが終了した「支払済み未登録」のケース）があれば、API登録→消費完了報告を実行する。

#### B. 端末アクティベーション（シート割当）
1. 管理者がWebの「シート・端末管理画面」で**端末アクティベーションコード**（ワンタイム・有効期限付き）を発行する。発行時に未割当シートが1つ引き当てられる。
2. 利用したいPCのブラウザでWebクライアントを開くと、未アクティベート端末では端末アクティベーション画面が表示される。ここでコードと端末名（例：「組立ライン1号機PC」）を入力する。
3. APIは端末（Device）レコードを作成してシートを割り当て、**デバイストークン**を発行する。トークンはブラウザに長期保持（HttpOnly Cookie）され、以降のAPI呼び出しで端末を識別する。DBにはトークンのハッシュのみ保存する。
4. アクティベーションコードは使用済みとなり再利用不可。

#### C. ログインとアクセス判定
1. アクティベート済み端末のブラウザからログイン画面へアクセスし、MESユーザーアカウントで認証してJWTを取得する。
2. 以降のAPI呼び出しはJWT（ユーザー認証）＋デバイストークン（端末認証）の両方をAPI側で検証する。どちらか欠ければ拒否。
3. **同一端末で複数のMESユーザーが利用可能**（交代勤務・共有端末を想定）。端末とユーザーは独立に管理される。
4. 例外として、システム管理者ロールによる「シート・端末管理画面」および初期セットアップ関連のAPI（本節E参照）のみ、未アクティベート端末からもアクセス可能とする（そうしないと最初の1台をアクティベートできないため）。

#### D. 割当解除・端末入替・トークン再発行
- 管理者はシート・端末管理画面から任意の端末の割当を解除できる。解除時点でその端末のデバイストークンは無効化され、シートは未割当プールに戻る（別端末への再割当が可能）。
- 1シートは常に最大1端末。**同一シートの複数端末での同時利用は構造上不可**（シート⇔端末は1対1、トークンは端末レコードと1対1）。
- ブラウザデータ消去等でデバイストークンを失った場合は、管理者が**再発行コード**を発行して同一端末を再アクティベートする（シートは消費しない。再発行時に旧トークンは無効化する）。
- **アクティベーションコードの失効とシート解放**：新規割当用コードが有効期限切れになった場合、または管理者が発行済み未使用コードを手動失効した場合、引当中のシートは自動的に未割当プールへ戻る（未割当シート数の計算で「発行中（未使用かつ有効期限内）」のコードのみを引当として数えるため、失効と同時に計算上プールに戻る。5章参照）。

#### E. 初期セットアップ（初期管理者アカウント）
- 初回起動時（Userテーブルが0件）のみ**初期セットアップAPI・画面**が有効になり、初期管理者アカウント（ユーザーID・パスワード）を作成できる。作成完了と同時に初期セットアップAPIは無効化される。
- 無人セットアップ（Docker／zip配布）向けに、環境変数または設定ファイルによる初期管理者の自動シードもサポートする（起動時にUserが0件の場合のみ適用し、シード用資格情報は初回ログイン時にパスワード変更を強制する）。

---

## 3. 機能モジュール詳細

### 3.1 生産管理（作業指示・実績）
- 作業指示（ワークオーダー）の発行・一覧・詳細確認
- 実績入力：ワークオーダー選択（またはQR/バーコードスキャンで自動選択）→ 良品数・不良数・作業時間・作業者を記録し、**産出ロット番号**（自動採番：品目コード＋日付＋連番、または手入力）を実績に紐付ける
- 進捗状況のリアルタイム表示（計画数に対する実績数、達成率）
- 日報・実績集計レポート出力（CSV/Excel）
- **製造日（業務日付）の定義**：日次集計・日報・ダッシュボードの「当日」は暦日ではなく**業務日付**で扱う。日付境界は既定で午前6時とし、設定で変更可能とする（夜勤の日跨ぎ実績が同一製造日に集計されるようにするため。実績の集計はすべてこの境界を用いる）
- **工順の方針**：複数工程を経る品目は**工程ごとに作業指示を分割して発行**する（WorkOrder＝品目×工程の単位）。初期リリースでは工程間の順序強制（前工程完了チェック）は行わず、工順マスタによる一括発行支援は将来拡張とする

### 3.2 在庫管理
- 入庫登録（入庫ロットの採番・記録を含む）
- 在庫照会（品目別・ロケーション別のリアルタイム在庫数）
- 出庫登録（**BOM（部品表）マスタに基づく生産実績連動の部材自動消費（バックフラッシュ）**、または出荷・手動出庫。連動出庫時は消費した部材ロットと作業指示を出庫履歴に記録する）
- BOMマスタ管理（親品目1単位あたりの子品目（部材）と必要数量を定義。バックフラッシュの根拠データ）
- 棚卸機能（実棚数入力と理論在庫との差異表示）

### 3.3 品質管理
- 検査記録入力（合格/不合格、不良項目、不良数、検査者）
- 不良分析（不良項目別・工程別・期間別の集計）
- トレーサビリティ：ロット番号による製造履歴の**前方・後方追跡**（部材ロット → 連動出庫（作業指示に紐付く）→ 作業指示 → 生産実績 → 産出ロット、の連鎖をたどる。逆方向も同様）。シリアル番号（個体）単位の追跡は初期リリースのスコープ外とし、将来拡張とする

### 3.4 設備管理
- 設備マスタ（資産番号、設置場所、稼働状態）
- 稼働履歴記録（稼働/停止/段取り替え等のステータス変更ログ）
- 保全記録（点検・修理履歴、次回保全予定）

### 3.5 共通基盤
- ユーザー管理・ロールベースアクセス制御（API側で一元管理）
- ログイン認証（ASP.NET Core Identity + JWT、API側に集約）
- ダッシュボード（当日実績サマリ、稼働率、不良率などのKPI表示）
- バーコード/QRスキャン共通コンポーネント（ブラウザのカメラAPI or USB HIDリーダー入力）
- **シート・端末管理**：API側で未割当シートプール・端末レコード・シート割当状態を保持し、Webクライアントのアクセス可否を端末単位で判定。シートの購入登録はWPFライセンスアプリから、アクティベーションコード発行・割当解除・再発行は管理者ロール専用のWeb管理画面から行う。

---

## 4. DBプロバイダー切替の設計（バックエンドのみ）

DBはバックエンド（MesApp.Api）のみが保持し、既定はSQLiteとする。EF Coreの`DbContext`をプロバイダー非依存に設計し、`appsettings.json`でプロバイダーと接続文字列を切り替えられるようにする。クライアント側はDB設定を一切持たない。

```json
{
  "Database": {
    "Provider": "Sqlite",   // "Sqlite" | "PostgreSql" | "SqlServer"
    "ConnectionString": "Data Source=mesapp.db"
  }
}
```

- `MesApp.Infrastructure`内でプロバイダーごとの`UseSqlite` / `UseNpgsql` / `UseSqlServer`を設定値に応じて切り替える。
- マイグレーションはプロバイダーごとに作成が必要（EF Coreの制約）。初期実装ではSQLiteのみ対応し、後続フェーズでPostgreSQL/SQL Server対応を追加する。
- SQLite利用時の同時アクセス対策：ASP.NET Coreは並行リクエストを処理するため、SQLiteでも書き込み競合は発生し得る。WALモードを有効化し、busy timeoutを設定し、書き込み競合時（SQLITE_BUSY）のリトライを実装する。数十端末規模まではこの構成で運用可能とするが、高負荷が想定される場合はPostgreSQL等への切替を推奨する旨をドキュメントに明記する。
- 在庫数量等の同時更新対策（プロバイダー非依存・アプリレイヤ）：`InventoryStock`など同時更新が起こり得るエンティティにはEF Coreの**楽観的同時実行制御（同時実行トークン／RowVersion）**を設定し、競合検出時は再読込・再計算のうえリトライする。マイナス在庫の防止判定は更新トランザクション内で行い、同時実績入力による在庫の二重引落し・引落し漏れを防ぐ。

---

## 5. データモデル（主要エンティティ）

| エンティティ | 主な項目 |
|---|---|
| User | ユーザーID、氏名、ロール、パスワードハッシュ |
| Product（品目マスタ） | 品目コード、品目名、単位、規格 |
| BomItem（部品表明細） | 親品目ID、子品目ID（部材）、必要数量（親1単位あたり） |
| Process（工程マスタ） | 工程コード、工程名、標準作業時間 |
| Equipment（設備マスタ） | 資産番号、設備名、設置場所、状態 |
| Location（棚卸ロケーション） | ロケーションコード、倉庫名 |
| WorkOrder（作業指示） | 指示番号、品目、工程、計画数、納期、状態 |
| Lot（ロット） | ロット番号（一意）、品目ID、初期数量、由来区分（生産/入庫）、生成元作業指示ID（生産ロットのみ）、製造日/入庫日 |
| ProductionRecord（生産実績） | 作業指示ID、作業者、良品数、不良数、開始/終了時刻、産出ロットID |
| InventoryTransaction（入出庫履歴） | 品目、数量、区分（入庫/出庫）、日時、関連ロットID、関連作業指示ID（生産連動出庫時） |
| InventoryStock（在庫） | 品目、ロケーション、現在数量 |
| QualityInspection（検査記録） | 対象ロットID、検査項目、判定、不良数、検査者 |
| EquipmentLog（設備稼働履歴） | 設備ID、ステータス、開始/終了時刻、備考 |
| SeatPurchase（シート購入履歴） | 購入トラッキングID（アプリ生成GUID、重複登録防止のため一意）、SKU（アドオン種別）、シート数、購入日、報告ユーザーID |
| SeatAdjustment（シート調整） | 調整数（正負。返金時はマイナス）、理由、調整日、実行者 |
| Device（端末） | 端末ID、端末名、デバイストークンハッシュ、登録日、最終アクセス日時、状態（有効/解除済み） |
| SeatAssignment（シート割当） | 割当先端末ID、割当元SeatPurchase ID、割当日、割当実行者、状態（有効/解除済み） |
| ActivationCode（アクティベーションコード） | コードハッシュ、種別（新規割当/トークン再発行）、対象端末ID（再発行時）、有効期限、状態（発行中/使用済み/失効）、発行者 |

- トレーサビリティの連鎖：産出ロット（`ProductionRecord.産出ロットID`）⇔ 作業指示 ⇔ 部材出庫（`InventoryTransaction.関連作業指示ID`＋`関連ロットID`）⇔ 部材ロット、のリレーションでロット単位の前方・後方追跡を実現する。
- シートと端末の関係：有効な`SeatAssignment`は端末（Device）と1対1。ユーザー（User）とは紐づけない。
- 未割当シート数 ＝ SeatPurchaseのシート数合計 ＋ SeatAdjustmentの調整数合計 − 有効なSeatAssignment数 − 発行中（未使用かつ有効期限内）の新規割当用ActivationCode数。コードの期限切れ・失効は計算上ただちにプールへの返却として反映される。

---

## 6. 画面一覧

### 6.1 Webクライアント（MesApp.Client.Web）

1. 端末アクティベーション画面（未アクティベート端末での初回アクセス時：コード・端末名入力）
2. ログイン画面
3. ダッシュボード（KPIサマリ、当日実績、稼働率）
4. 作業指示一覧・詳細
5. 実績入力画面（バーコード/QRスキャン対応）
6. 在庫照会・入出庫登録画面
7. 品質検査入力・不良分析画面
8. 設備管理・稼働履歴画面
9. マスタ管理画面（品目／BOM（部品表）／工程／設備／ユーザー）
10. シート・端末管理画面（管理者専用：未割当シート残数・購入履歴・調整履歴表示、シート調整（返金等のマイナス調整）、端末一覧［端末名・割当日・最終アクセス］、アクティベーションコード発行・発行済み未使用コードの失効、割当解除、トークン再発行コード発行）
11. 初期セットアップ画面（Userが0件の初回起動時のみ有効：初期管理者アカウント作成）

### 6.2 WPFライセンスアプリ（MesApp.LicenseApp）

1. バックエンド接続設定画面（API URL設定・疎通確認）
2. 管理者ログイン画面
3. シート購入画面（SKU選択（シート ×1／×5／×10）、Store In-App Purchase起動、購入結果のバックエンド登録、未完了処理（API未登録・消費報告未完了）のリカバリ状況表示、登録済み購入履歴の表示）

---

## 7. 非機能要件

### 7.1 Windows Store公開関連（WPFライセンスアプリ）
- WPFアプリをMSIXパッケージとしてビルド（パッケージプロジェクト or `WindowsPackageType=MSIX`）。Store IAPの利用にはパッケージアイデンティティが必須
- Microsoft Store Partner Centerでアプリ登録、**消費型（Store-managed consumable）Add-on**として「シート ×1」「シート ×5」「シート ×10」等の複数SKUを設定（Store IAPは1回の購入で数量を指定できないため、まとめ買いはSKUの粒度で対応する）
- 購入は`Windows.Services.Store`名前空間のAPI（`StoreContext.RequestPurchaseAsync`）で行う。購入開始前に購入トラッキングID（アプリ生成GUID）をローカルに永続化し、購入成功後にMesApp.Apiへ登録して未割当シートプールへ加算、API登録成功後に`ReportConsumableFulfillmentAsync`（trackingIdに同GUIDを指定）でStoreへ消費完了を報告する
- リカバリ処理（起動時）：消費報告未完了分の再送に加え、`GetConsumableBalanceRemainingAsync`でStore側の未消費残高を照会し、購入成功後・API登録前にアプリが終了した「支払済み未登録」分を検出してAPI登録→消費報告を行う
- 購入トラッキングID（アプリ生成GUID）をAPI側で一意制約とし、同一購入の二重登録を防止する（同一IDの再送には冪等に成功応答を返す）。`StorePurchaseResult`はStore発行のトランザクションIDをアプリに公開しないため、Store発行IDには依存しない
- WPF（デスクトップアプリ）からの`StoreContext`取得はウィンドウハンドルの関連付け（`IInitializeWithWindow`）が必要な点に留意する
- 将来オプション：WPFアプリ（ネット接続可）がベンダー運用のライセンスサービスにStoreレシートを送って検証し、署名付きエンタイトルメントを取得してバックエンドに登録する方式（バックエンドは埋め込み公開鍵でオフライン署名検証）。初期リリースではスコープ外
- コード署名、Microsoft Store認定ポリシーへの準拠
- **Storeポリシー適合の事前確認**：アプリ内で価値を消費せず外部システム（オンプレMES）のライセンスをIAPで販売する本モデルがMicrosoft Store認定ポリシー（10.8系）に適合するかは自明でないため、**Phase 9を待たずプロジェクト初期に**Partner Centerでのポリシー確認（必要に応じて最小構成アプリでの審査通過確認）を実施する。不適合と判明した場合は代替方式（ベンダー直販のライセンスキー方式等）へ課金モデルを再検討する

### 7.2 配布形態
- **Windows Store（MSIX）**: MesApp.LicenseApp（WPFライセンスアプリ）の配布経路
- **Zip配布**: MesApp.Api（バックエンド、Webクライアント静的ファイル同梱）のビルド済みバイナリをzip化し、社内サーバー等に手動配置できるようにする
- **Docker配布**: MesApp.Api（Webクライアント同梱）および既定DBをDockerイメージ化し、`docker-compose.yml`で一括起動できるようにする（バックエンドのセルフホスト手段として）
- MesApp.Client.Webは必ずMesApp.Apiが配信する（別配信は行わない。端末アクティベーション制御と配信を一体化するため）

### 7.3 オフライン動作について
- 本構成はクライアントがAPI経由でしかDBにアクセスしないため、API・DBがダウンしているとクライアントは操作不能になる。
- 完全オフライン対応が必要な場合は、WebクライアントのPWA化（Service Workerによるキャッシュと再送信キュー）を将来検討する（初期リリースではスコープ外とし、Phase以降で拡張）。

### 7.4 セキュリティ
- 認証はASP.NET Core Identity + JWT、API側に集約
- **JWTの構成と保管**：アクセストークン（短命、既定60分）＋リフレッシュトークン（既定12時間、交代勤務のシフト長を考慮）で構成する。アクセストークンはWebクライアントのメモリ上にのみ保持し、localStorage等の永続ストレージには保存しない（XSS時の窃取面を最小化）。リフレッシュトークンはHttpOnly Cookieで保持し、サイレント更新する
- **CSRF対策**：状態変更APIはすべてAuthorizationヘッダのJWTを必須とし（Cookieのみでは実行不可）、Cookie（デバイストークン・リフレッシュトークン）には`HttpOnly`・`Secure`・`SameSite=Strict`を設定する。トークンリフレッシュ等Cookieのみで動作するエンドポイントへのクロスサイトからの発火はSameSite属性で遮断する
- 通信は全てHTTPS。バックエンドはローカル（社内）環境で稼働するため公的CA証明書は取得できない前提とし、プライベートCA（社内CA or mkcert等）で発行した証明書をサーバーに設定し、CA証明書をクライアント端末（ブラウザ利用端末・WPFアプリ利用端末）に配布・信頼させる運用手順をドキュメント化する
- ロールベースアクセス制御はAPI側で一元的に判定（クライアント側の表示制御はUXのためのみで、最終判定は常にAPI側）
- 端末のアクセス可否判定は必ずAPI側で行う。判定材料はバックエンドDB内のシート割当状態とデバイストークン（ハッシュ照合）のみ
- デバイストークンはHttpOnly Cookieとしてブラウザに保持する。ブラウザからはハードウェアに強固にバインドできないため、Cookieの複製による別端末利用は技術的には可能である。オンプレB2B環境では顧客側の意図的な不正を要するためリスク許容とし、緩和策として同一デバイストークンの異常利用検知（異なるIPアドレス/User-Agentからの同時アクセスの警告ログ・管理画面表示）をオプション実装する。より強固な端末バインド（TLSクライアント証明書等）は将来オプションとする
- シート購入登録・シート調整・アクティベーションコード発行/失効・割当解除・トークン再発行のAPIは管理者ロール専用とし、操作を監査ログ（7.6節）に記録する
- アクティベーションコードはワンタイム・有効期限付きとし、DBにはハッシュのみ保存する。コードは暗号論的乱数で生成し十分なエントロピー（128bit相当。例：Crockford Base32で26文字、視認性のためハイフン区切り）を持たせる。コード入力APIには送信元（IPアドレス）単位のレート制限と連続失敗時の一時ロックアウトを設け、総当たりを防ぐ
- 前提として、バックエンドは顧客管理下のオンプレ環境で動作するため、ライセンス強制は完全な技術的保護ではなくリスク許容ベースである（1.4節参照）

### 7.5 性能・規模
- 想定同時接続端末数、日次実績データ件数などは導入規模に応じて別途設定（初期は数十端末規模を想定）

### 7.6 ログ・監査
- 操作ログ（誰が・いつ・何を変更したか）をAPI側で記録し、追跡可能にする。シート・端末管理操作（購入登録・調整・割当・解除・再発行・コード失効）は必須記録とする

### 7.7 バックアップ・リストア
- SQLite利用時：稼働中のオンラインバックアップ手段（`VACUUM INTO` またはSQLite Backup API）を用いた定期バックアップ手順を提供する。WALモードで稼働するため、**稼働中のDBファイル単純コピーは行わない**ことを運用手順書に明記する。Docker構成ではバックアップ用ボリュームとバックアップスクリプトを`docker-compose.yml`に同梱する
- PostgreSQL/SQL Server利用時：各DBの標準手段（`pg_dump`等）によるバックアップを前提とし、推奨手順をドキュメント化する
- リストア手順（バックアップファイルの差し替え→API再起動）と、リストアの影響範囲（デバイストークン・シート割当・実績データがバックアップ時点へ巻き戻る。リストア後に端末の再アクティベートが必要になり得る）を運用手順書に明記する

---

## 8. 技術スタック

| 分類 | 技術 |
|---|---|
| Webクライアント | Blazor WebAssembly（.NET 10（LTS）、MES機能の唯一のクライアント） |
| ライセンスアプリ | WPF（.NET 10（LTS）、Windows Store配布、シート購入・登録専用） |
| バックエンドAPI | ASP.NET Core Web API（.NET 10（LTS）、Webクライアント静的配信を含む） |
| ORM | Entity Framework Core（プロバイダー切替対応、バックエンドのみ） |
| 既定DB | SQLite |
| 追加対応DB | PostgreSQL（Npgsql）、SQL Server |
| 認証 | ASP.NET Core Identity + JWT（APIに集約）＋ デバイストークン（端末認証） |
| バーコード/QRスキャン | ブラウザカメラAPI or USB HIDリーダー入力 |
| ライセンス管理 | Windows.Services.Store API（Consumableアドオン購入・消費報告、WPFアプリ側）＋ API側でのシートプール・端末割当管理 |
| パッケージング | MSIX（Windows Store、WPFライセンスアプリ）、自己完結型Zip（API）、Dockerイメージ（API） |

※ .NET 8はLTSサポートが2026年11月に終了するため採用せず、現行LTSの.NET 10で統一する。

---

## 9. ディレクトリ構成案

```
/ParallelFactoryMES
  /src
    /MesApp.Client.Web           … Blazor WebAssembly（MES機能クライアント）
      /Pages
      /Components
      /Services                  … APIクライアント、スキャン処理
    /MesApp.LicenseApp           … WPF（Windows Store配布、シート購入・登録専用）
      /Views
      /Services                  … StoreライセンスAPI連携、APIクライアント
    /MesApp.Api                  … ASP.NET Core Web API（バックエンド本体、Webクライアント配信）
      /Controllers
      /Services                  … 業務ロジック、シート・端末管理、ライセンス検証
      Dockerfile
    /MesApp.Core                 … 共通ドメインモデル・DTO・APIコントラクト
    /MesApp.Infrastructure        … EF Core、DBプロバイダー切替、リポジトリ実装（API専用）
  /tests
    /MesApp.Api.Tests
  /docker
    docker-compose.yml            … MesApp.Api + DB（必要に応じ）を一括起動
  /docs
    仕様書.md（本ドキュメント）
```

---

## 10. 開発フェーズ（マイルストーン）

| フェーズ | 内容 |
|---|---|
| Phase 1 | バックエンド基盤構築（MesApp.Api / Core / Infrastructure、EF Core+SQLiteでのDB設計・マイグレーション、JWT認証（アクセス＋リフレッシュトークン）、初期管理者セットアップ） |
| Phase 2 | 生産管理API（作業指示・実績入力・進捗・産出ロット採番）の実装 |
| Phase 3 | 在庫管理API（入出庫・在庫照会・棚卸、BOMマスタとバックフラッシュ出庫） |
| Phase 4 | 品質管理API（検査記録・不良分析・ロットトレーサビリティ） |
| Phase 5 | 設備管理API |
| Phase 6 | MesApp.Client.Web（Blazor WebAssembly）のUI実装（作業指示一覧・実績入力から着手）、MesApp.Apiからの静的配信 |
| Phase 7 | Webクライアント残画面の実装、バーコード/QRスキャン統合（ブラウザカメラAPI / USB HIDリーダー） |
| Phase 8 | シート・端末管理の実装（シートプール・Device・SeatAssignment・ActivationCode・SeatAdjustmentのAPI、端末アクティベーションフロー、コード失効時のシート解放、デバイストークン検証ミドルウェア、シート・端末管理画面） |
| Phase 9 | MesApp.LicenseApp（WPF）の実装（Store Consumable IAP（複数SKU）、購入トラッキングIDによる購入結果のAPI登録、消費報告・未登録残高のリカバリ）、MSIXパッケージング・Store公開 |
| Phase 10 | DBプロバイダー切替対応（PostgreSQL/SQL Server）、Dockerイメージ化、Zip配布パッケージ作成、最終テスト |

※ Microsoft Storeポリシー適合の事前確認（7.1節）は、課金モデルの成立可否に関わるためPhase 9を待たず**Phase 1と並行して**実施する。

---

## 11. Claude Codeへの実装依頼時の進め方（推奨）

本仕様書をベースに、フェーズごとに区切って実装を依頼することを推奨する。

**Phase 1の指示例:**
> この仕様書（Spec.md）を読み込んだ上で、Phase 1（バックエンド基盤構築）を実施してください。ソリューション構成（MesApp.Api / MesApp.Core / MesApp.Infrastructure）を作成し、EF Core + SQLiteでのDBモデル定義・マイグレーション、ASP.NET Core Identity + JWT（アクセス＋リフレッシュトークン、7.4節）による認証基盤、初期管理者セットアップ（2.2節E：Userが0件時のみ有効なセットアップAPI＋環境変数シード）を実装してください。DBプロバイダーは設定ファイルで切替可能な形で抽象化してください。

**Phase 6の指示例（Webクライアント着手時）:**
> Phase 1〜5で実装済みのMesApp.Apiを呼び出す形で、MesApp.Client.Web（Blazor WebAssembly）に作業指示一覧・実績入力画面を実装し、MesApp.Apiから静的配信できるようにしてください。

**Phase 8の指示例（シート・端末管理着手時:**
> 仕様書2.2節・5章に基づき、シートプール・端末アクティベーション・デバイストークン検証を実装してください。管理者による端末アクティベーションコード発行〜対象PCブラウザでのアクティベート〜デバイストークンCookie発行〜API呼び出し時のJWT＋デバイストークン二重検証、までを通しで動作させてください。

**Phase 9の指示例（WPFライセンスアプリ着手時）:**
> シート購入・登録専用のWPFアプリ（MesApp.LicenseApp）を作成してください。バックエンドURL設定・管理者ログイン・Windows Store Consumableアドオン（複数SKU）の購入（Windows.Services.Store）・購入トラッキングID（購入前にローカル永続化するアプリ生成GUID）による購入結果のMesApp.Apiへの登録・消費完了報告を実装してください。起動時リカバリ（消費報告未完了分の再送、GetConsumableBalanceRemainingAsyncによる支払済み未登録分の検出・登録）も含めてください。

各フェーズ完了後に動作確認を行い、次フェーズへ進む。

---

## 12. 参考

- みんなのMES（min-MES）: https://min-mes.com/
- OSSリポジトリ: https://github.com/mihatama/open-mes-project
