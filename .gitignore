# Build outputs
bin/
obj/
*.user

# IDE
.vs/
.idea/

# Runtime artifacts (never commit: local DB, JWT signing key)
*.db
*.db-shm
*.db-wal
jwt-signing.key

# Test results
TestResults/

# Claude Code の設定のうち、端末ごとに増える許可リストだけ除外する
# （launch.json・settings.json・commands/ は CLAUDE.md が前提にしているのでコミットする）
.claude/settings.local.json


# 第三者著作物（一財）エンジニアリング協会『MES/MOM導入のための標準業務一覧』
# 著作権はENAAに帰属するため公開リポジトリには含めない
# 入手先: https://www.enaa.or.jp/research/smart/mes
MES.md

ref/
artifacts/
参考/
参考2/

# ストア掲載画像の素材（アプリの実画面。build/Capture-AppScreens.js でいつでも撮り直せる）
build/store/raw/
