# Claude Code オーケストレーション運用仕様書
プロダクト名：Parallel Factory MES

版: 1.0（2026-08-22 制定）
対象: 本リポジトリにおける Claude Code を用いた開発作業すべて

---

## 1. 目的とゴール

### 1.1 目的
Claude Code の使用量（トークン／使用枠）を削減しつつ、成果物の品質を維持・向上させる**開発作業の進め方**を仕様として定義する。本書は製品仕様（Spec.md）ではなく、製品を作る**プロセスの仕様**である。

### 1.2 基本思想
消費量の削減は「安いモデルを使う」ことでは達成しない。**手戻り1回のコストが、丁寧な計画10回分を上回る**ため、品質を落とす節約は総消費量を増やす。本書の節約レバーは以下の4つに限定する。

1. **文脈を小さく保つ**（毎ターン再送される履歴を短く保つ）
2. **入力を絞る**（読むべき行だけを読む）
3. **出力を絞る**（ビルド／テストの生ログを流し込まない）
4. **手戻りをゼロにする**（計画と受入条件の先行定義）

### 1.3 目標値（KPI）
| 指標 | 目標 |
|:--|:--|
| 1タスクあたり総トークン | 現行比 −50% |
| 手戻り率（受入不合格 → 再実装） | 5% 以下 |
| マージ前のビルド／テスト成功率 | 100% |
| 1タスクあたりのファイル読込行数 | 1,500行以下 |

### 1.4 非目標
- モデルの一律ダウングレードによる節約（§8 で禁止）
- 人手作業への回帰（自動検証は積極的に使う）

---

## 2. コスト構造（なぜ節約できるのか）

### 2.1 モデル単価
| モデル | モデルID | 入力 $/1M | 出力 $/1M |
|:--|:--|--:|--:|
| Claude Opus 5 | `claude-opus-5` | $5.00 | $25.00 |
| Claude Sonnet 5 | `claude-sonnet-5` | $3.00 | $15.00 |
| Claude Haiku 4.5 | `claude-haiku-4-5` | $1.00 | $5.00 |

キャッシュ読取は入力単価の約 0.1 倍、キャッシュ書込は約 1.25 倍。**先頭文脈を変えずにセッションを継続すると、入力コストは概ね 1/10 に落ちる**。

### 2.2 会話は毎ターン全量が再送される
N ターン目の入力は「それまでの全履歴」である。したがって 1 セッションの総入力トークンは概ね**ターン数の二乗**に比例する。

- 無関係なタスクを 1 セッションで連続実行すると、後半のタスクは前半の履歴すべてを毎ターン支払う。
- **1セッション＝1タスク**が単独で最も効く節約策である理由がこれ。

### 2.3 本リポジトリの実測規模
| 対象 | 規模 | 備考 |
|:--|--:|:--|
| src + tests（.cs / .razor） | 179ファイル / 39,306行 | 全読込で概算 50万トークン相当 |
| うち `Migrations/`（Designer/Snapshot 含む） | 約13,700行（35%） | **読む価値ゼロ。読込禁止** |
| Spec.md | 496行 | 該当節のみ grep |
| MES.md | 518行 | 該当業務Noのみ grep |
| docs/*.md | 3,321行 | 該当ファイルのみ |

コードベース全体を1回舐めるだけで Opus 入力 $2.5 相当が飛ぶ。**探索の規律が最大の効き目を持つ**。

### 2.4 レバー別の期待効果（推定値・実測で更新すること）
| レバー | 削減率 | 品質への影響 |
|:--|--:|:--|
| 1セッション1タスク（§4.2） | 30〜50% | ↑ 文脈汚染が減り誤修正が減る |
| 探索規律（§4.3） | 20〜40% | ↑ 無関係な情報に引きずられない |
| 出力フィルタ（§4.4） | 5〜15% | → 変化なし |
| L3（非LLM）優先（§3.2） | 5〜10% | ↑ 決定論的で確実 |
| L2 ルーティング（§3.3） | 10〜20% | 条件付き（§5.1 DoD と §5.2 エスカレーションで担保） |

---

## 3. 役割モデル（オーケストレーション）

### 3.1 3層構造
| 層 | 担当 | 使用モデル | 責務 | やらないこと |
|:--|:--|:--|:--|:--|
| **L1 オーケストレーター** | メインセッション | Opus 5 | 分解・設計判断・タスクカード発行・受入判定・統合 | 大量のコードを自分で書かない／全文を読まない |
| **L2 ワーカー** | 別セッション or サブエージェント | Sonnet 5 | 仕様が確定した実装、定型追加、文書追記 | 設計判断・仕様変更の独断 |
| **L3 メカニカル** | ツール実行のみ | なし（**トークン0**） | ビルド、テスト、grep、置換、EF migration 生成、整形 | 判断 |

### 3.2 大原則
> **L3 で済むことを LLM にさせない。L2 で済むことを L1 にさせない。**
> ただし「済むかどうか」の判定は L1 が行う。判定を誤ると手戻りが発生し、節約分は消える。

### 3.3 ルーティング表（本プロジェクトのタスク類型）
| タスク類型 | 層 | 根拠 |
|:--|:--|:--|
| データモデル変更（Core/Entities、DbContext） | L1 | 波及範囲が最大。手戻りコストが最も高い |
| 新規業務モジュールの設計（Spec.md §3/§5/§6 の改訂を伴う） | L1 | 仕様の一貫性判断が必要 |
| 認証・在庫更新・実績/原価計算・CSV取込のロジック変更 | L1 | 誤りが業務データを壊す |
| Controller + Service + テストの実装（仕様確定済） | L2 | 既存パターンの反復 |
| Razor ページのタブ／画面追加（既存タブを踏襲） | L2 | テンプレート模倣 |
| マスタCSVの項目追加 | L2 | `MasterCsvService` の既存構造に従う |
| docs/*.md への操作手順の追記 | L2 | 文体の踏襲のみ |
| 一括リネーム・定型置換 | L3 | sed / IDE のリファクタで確実 |
| EF Core マイグレーション生成 | L3 | `dotnet ef migrations add` |
| 「どこで使われているか」調査 | L3 → 不足時のみ L2 | まず grep。§4.5 の条件を満たす場合のみサブエージェント |
| ビルドエラー修正 | L2（2回失敗で L1 へ） | §5.2 |

---

## 4. 実行プロトコル

### 4.1 タスクカード（作業の唯一の入力単位）
すべての作業は以下の形式のタスクカードから開始する。探索コストの大半は「どこを触ればよいか分からない」ことから発生するため、**カードの精度がそのまま消費量に直結する**。

```
## タスク: <ID> <一行要約>
- 目的 / 背景:
- 変更対象: <ファイルパスを列挙。未確定なら「調査で確定」と明記し、調査範囲を限定する>
- 変更禁止: <例: Migrations/ 配下、既存API契約、DbContext のスキーマ>
- 受入条件(DoD): <機械的に検証できる形で箇条書き>
- 参照: Spec.md §x.y / MES.md No.X-XX-XX-XX / docs/xxx.md
- 想定層: L1 / L2 / L3
- 想定変更ファイル数: N
```

**規則**
- 「変更対象」が3ファイルを超える、または「調査で確定」が2項目以上あるカードは**粒度が粗い**。分割する。
- カードなしで大きな変更に着手しない（§8-8）。

### 4.2 セッション境界規則
| 状況 | 操作 |
|:--|:--|
| タスクカード1枚が完了した | `/clear` |
| 次のタスクが前タスクのファイルを1つも共有しない | `/clear`（キャッシュ利得がないため） |
| 次のタスクが同じファイル群を触る | 継続（キャッシュ読取 0.1倍の恩恵を受ける） |
| 30ターンを超えた | 受入条件を確認して打ち切り、残りを新カードに切り出す |
| 作業を続けたいが文脈が長い | `/compact`（継続に文脈が本当に必要な場合のみ） |

`/clear` はキャッシュを捨てる操作でもある。**「無関係なら clear、関係あるなら継続」**が判断基準であり、機械的な頻繁 clear は逆効果。

### 4.3 探索フェーズ規約（最重要）
**手順（この順序を厳守）**
1. `Glob` でファイルを特定する
2. `Grep`（`-n`、パス限定）で該当行を特定する
3. 該当行の **±40行のみ** を `Read` する（`offset` / `limit` 指定）

**全文読込を禁止する対象**
- `src/MesApp.Infrastructure/Migrations/**`（`*.Designer.cs`、`MesAppDbContextModelSnapshot.cs` を含む）
- `Spec.md`、`MES.md`（必ず grep で該当節・該当業務Noのみ）
- 500行を超える任意のファイル（`Maintenance.razor`、`MasterCsvService.Import.cs` 等）

**探索予算（1タスクあたり）**
- 読込 1,500行以下 / Grep 8回以下
- 超過した時点で「タスクカードの粒度が粗い」と判定し、**探索を止めてカードを再定義する**（そのまま続けるのが最も高くつく）

### 4.4 出力削減規約（ビルド／テスト）
生ログは投入しない。以下のフィルタ済みコマンドを標準とする。

```powershell
dotnet build ParallelFactoryMES.slnx -v q --nologo 2>&1 | Select-String -Pattern "error|warning CS|Build succeeded|ビルドに成功" | Select-Object -First 40
```

```powershell
dotnet test tests/MesApp.Api.Tests -v q --nologo 2>&1 | Select-String -Pattern "error|Failed|Passed!|成功!|失敗|合計" | Select-Object -First 40
```

**規則**
- 常に `-v q --nologo`。
- テストが落ちた場合のみ、該当テストを単体で再実行して詳細を取る:
  `dotnet test tests/MesApp.Api.Tests --filter FullyQualifiedName~<TestClass> -v n`
- ブラウザ確認（preview）は実装完了後の最後の1回に集約する。途中経過のスクリーンショットは撮らない。

### 4.5 サブエージェント起動基準
サブエージェントは**コールドスタートで文脈を再取得する固定費**が高い。人的な待ち時間は短縮できても、トークンは増えることが多い。**「節約のための分割」は原則として逆効果**。

**起動してよい条件（すべて満たす場合のみ）**
1. 10ファイル以上を横断走査する調査であり、生の中身は不要で結論だけが要る
2. タスクが自己完結しており、現在の会話文脈をほぼ必要としない
3. 生の走査結果を親セッションに残さない方が、後続ターンの再送コストを下げる

**起動してはいけない場合**
- 単一ファイル／数回の grep で終わる調査
- すでに文脈がある作業の続き
- 実装の分担（統合コストがトークン・品質の両面で上回る）

### 4.6 計画と実装の分離
1. L1 が計画を立て、タスクカードに落とす
2. 人間が承認する（唯一かつ最も安い品質関門）
3. L2 が実装する
4. L3（build / test）で検証する
5. L1 が受入判定する

計画フェーズを飛ばして得られる節約は、手戻り1回で失われる。**§1.2 の第4レバー（手戻りゼロ）は、品質と節約が同時に成立する唯一の項目**である。

---

## 5. 品質ゲート（節約しても品質を落とさないための担保）

### 5.1 DoD（全タスク共通・必須）
以下をすべて満たさない限り「完了」と報告しない。

1. `dotnet build` 成功、かつ**新規の警告が増えていない**
2. 該当プロジェクトの `dotnet test` が全緑
3. 変更が仕様に触れる場合、**Spec.md を同一コミットで更新**（改訂履歴行も追加）
4. 変更が MES.md のどの業務プロセス（No）に対応するか説明できる
5. 実変更ファイル数がタスクカードの想定と一致する（**乖離は設計ミスの信号**として L1 に差し戻す）

### 5.2 エスカレーション規則
- **同一のエラーで2回失敗したら、3回目を同じ層で試さない。**（最も無駄な消費パターン）
- L2 → L1 への引き継ぎは会話履歴ではなく、以下の要約のみを渡す:
  - 試したこと（箇条書き）
  - エラー全文（フィルタ後）
  - 現時点の仮説
- L1 でも2回失敗したら、タスクカードの前提（変更対象・受入条件）自体を疑う。

### 5.3 レビューの起動条件
コードレビューは常時ではなく、以下のいずれかに該当する場合に限る。
- 差分が 200行 または 3ファイルを超える
- 認証／在庫更新／実績・原価計算／CSV取込／マイグレーション に触れる
- `main` へのマージ前

`/code-review` は通常レビュー、`/code-review ultra` は `main` マージ前に限定する（ultra は課金される多エージェントレビューであり、起動は利用者が行う）。上記に該当しない差分は §5.1 の DoD で足りる。

### 5.4 テスト方針（節約と直結）
- 受入条件をテストとして先に書けるものは先に書く。→ 検証ループが **L3（トークン0）**で回るようになり、試行錯誤のコストが消える。
- 既存テスト（`tests/MesApp.Api.Tests`）のパターンを踏襲し、新規のテスト基盤は作らない。

---

## 6. リポジトリ側の資産（コールドスタートを安くする）

毎回の「このプロジェクトは何か」の再学習は純粋な無駄。以下をリポジトリに置いて固定費を下げる。

### 6.1 CLAUDE.md（新規作成・必須）
**100〜150行以内**。毎ターン全量が再送されるため、**1行の冗長が全ターンに課金される**。

記載するもの:
- ビルド／テスト／実行コマンド（§4.4 のフィルタ済み形式で）
- プロジェクト構成（4プロジェクトの責務を各1行）
- **読込禁止パス**（`Migrations/` 等、§4.3）
- コーディング規約の要点（既存パターン踏襲、命名、Razor の書き方）
- Spec.md / MES.md の参照方法（「全文を読まず grep せよ」）

記載しないもの:
- 仕様の内容（Spec.md の写し）、業務プロセス表（MES.md の写し）、変更履歴

### 6.2 `.claude/settings.json` の許可リスト
承認ダイアログの往復は、人の時間とターンの両方を消費する。読取専用・冪等なコマンドは許可リストに入れる。

候補: `dotnet build`, `dotnet test`, `dotnet ef migrations list`, `git status|diff|log`, `Select-String`, `Get-ChildItem`
生成には `/fewer-permission-prompts` を使う。

### 6.3 コードマップ（`docs-dev/CodeMap.md`）
「業務機能 → 実装ファイル」の対応表。§4.3 の探索3手順を**1回の Read に置換する**ための資産。

- 行: MES.md の Division（生産管理／製造実行／品質管理／物流在庫／設備保全／従業員）
- 列: Entity / DbContext設定 / Controller / Service / Razorページ / テスト
- 更新責任: 新規モジュール追加タスクの DoD に「CodeMap 更新」を含める
- 配置先: 公開サイト（`docs/`）ではなく開発者向けの `docs-dev/` とする

### 6.4 カスタムスラッシュコマンド（候補）
| コマンド | 内容 |
|:--|:--|
| `/task-card` | §4.1 の雛形を生成し、対象ファイルの候補を grep で埋める |
| `/verify` | §4.4 のビルド＋テストをフィルタ実行し、§5.1 の DoD 判定を返す |
| `/add-master` | マスタ画面追加の定型手順（Entity → DbContext → Controller → Tab → CSV → テスト）を順に実行 |

### 6.5 `.claude/launch.json`
既存の `mesapp` 構成（`dotnet run --project src/MesApp.Api`、port 5210）を使う。**シェルから dev サーバを起動しない**（プロセスが残り、ログが文脈を汚す）。

---

## 7. 計測と改善

### 7.1 記録項目（タスクカード完了時に1行追記）
`タスクID / 層 / ターン数 / 読込行数 / 手戻り回数 / 所感`

### 7.2 レビュー周期
週次で以下を確認し、本仕様に反映する。
- 読込行数が多かったタスク上位3件 → §6.3 コードマップに追記して次回の探索を消す
- 手戻りが発生したタスク → 原因が「カードの粒度」か「層の誤ルーティング」かを判定し、§4.1 か §3.3 を更新
- §2.4 の推定削減率を実測値で置き換える

---

## 8. アンチパターン（禁止事項）

1. **節約目的でモデルを一律ダウングレードする** — 手戻りが増え、総消費量はむしろ増える
2. **「まず全体を把握して」から始める** — 全文読込は 50万トークン級（§2.3）
3. **1セッションで無関係なタスクを連続実行する** — 履歴再送が二乗で効く（§2.2）
4. **同じ失敗を3回試す** — 2回で必ずエスカレーション（§5.2）
5. **節約目的のサブエージェント乱発** — コールドスタート固定費で逆効果（§4.5）
6. **生のビルド／テストログを投入する** — §4.4 のフィルタを必ず通す
7. **CLAUDE.md に仕様を丸写しする** — 全ターンに課金される（§6.1）
8. **タスクカードなしで大きな変更に着手する** — 探索が発散し、手戻りを誘発する
9. **DoD 未達で「完了」と報告する** — §5.1 の全項目が唯一の完了定義

---

## 9. 導入フェーズ

| フェーズ | 期間 | 内容 | 完了条件 |
|:--|:--|:--|:--|
| Phase 1 | 即日 | CLAUDE.md 作成（§6.1）、settings 許可リスト（§6.2）、フィルタ済みコマンドの適用（§4.4） | CLAUDE.md が150行以内で存在し、ビルド／テストがフィルタ経由で実行される |
| Phase 2 | 1週間 | タスクカード運用（§4.1）、セッション境界規則（§4.2）、DoD（§5.1）の適用 | 全タスクがカード起点で開始される |
| Phase 3 | 2週間 | コードマップ（§6.3）、カスタムコマンド（§6.4）の整備 | 探索フェーズが平均500行以下で完了する |
| Phase 4 | 継続 | 計測（§7）とルーティング表（§3.3）の更新 | §1.3 の KPI を4週連続で達成 |

---

## 付録A. 作業開始時の判断フロー

```
タスクが来た
 │
 ├─ 機械的に完結する？（置換・生成・調査コマンド）
 │   └─ YES → L3 で実行。LLM を使わない。
 │
 ├─ タスクカードがある？
 │   └─ NO → L1 でカードを作る（§4.1）。ここで止まるのが最も安い。
 │
 ├─ 設計判断 or 高リスク領域を含む？（§3.3）
 │   ├─ YES → L1 で計画 → 承認 → L2 で実装
 │   └─ NO  → L2 で実装
 │
 ├─ 前タスクとファイルを共有する？
 │   ├─ YES → 同一セッションを継続（キャッシュ利得）
 │   └─ NO  → /clear してから開始
 │
 └─ 実装後: L3 で build + test（§4.4）→ DoD 判定（§5.1）
     └─ 2回失敗 → 上位層へエスカレーション（§5.2）。3回目を同じ層で試さない。
```
